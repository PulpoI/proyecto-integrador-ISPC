export interface Product {
  
  id: number;
    name: string;
    description: string;
    price: number;
    image: string;
    quantity: number;
    category: string;
   // Make sure the 'product' property is defined correctly

    colors: string[]; // Define the 'colors' property and assign the appropriate value
    sizes: string[]; // Define the 'sizes' property and assign the appropriate value
  
  }

  